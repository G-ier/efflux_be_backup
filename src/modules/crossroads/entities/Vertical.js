class Vertical {
    constructor(id, name, provider_id, provider) {
        this.id = id;
        this.name = name;
        this.provider_id = provider_id;
        this.provider = provider;
    }
}

module.exports = Vertical;
