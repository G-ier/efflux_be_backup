// Logically the costs should be updated after the traffic sources and networks are updated.
const FUNNEL_FLUX_REGULAR_COST_UPDATE_CRON = '4,19,34,49 * * * *';

module.exports = {
  FUNNEL_FLUX_REGULAR_COST_UPDATE_CRON
};
